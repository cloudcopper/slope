+++
status = "done"
+++
# Basic cobra application

Create basic cobra based application cmd/slope. Take all needed packages.
Make at root Makefile to build the app (shall produce app at root of project).
Use golang related skills.
Add the tests, if needed (use related golang testing skills).

# Expected outcome

[x] binare slope at root of project
[x] Makefile with target build creating slope app
